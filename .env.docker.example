# Salin ke .env.docker:  make env-docker   (atau cp manual). .env.docker TIDAK di-commit.
# Dipakai docker compose lewat --env-file (root Makefile sudah pasang flag ini).
# Samakan nilainya dengan DATABASE_URL di apps/backend/.env.

POSTGRES_USER=questday
POSTGRES_PASSWORD=questday
POSTGRES_DB=questday
POSTGRES_PORT=5432
