# Frontend

TODO — belum dikerjakan.

Rencana: konsumsi API dari `apps/backend` sesuai kontrak di `contracts/openapi.yaml`.
Generate client/type dari kontrak itu biar sinkron dengan backend.

Saat mulai, tambahkan `.gitignore` sendiri di folder ini (root .gitignore sengaja minimal).
