package scoring

// handler.go = lapisan HTTP scoring (tipis).
//
// TODO:
//   type handler struct { svc *service }
//   func newHandler(svc *service) *handler
//   func (h *handler) score(w, r)        // GET /me/score
//   func (h *handler) streak(w, r)       // GET /me/streak
//   func (h *handler) leaderboard(w, r)  // GET /leaderboard
