package scoring

// repository.go = kontrak penyimpanan scoring.
//
// TODO:
//   type Repository interface {
//       GetWallet(ctx, userID string) (*Wallet, error)     // buat default bila belum ada
//       SaveWallet(ctx, *Wallet) error
//       AddTransaction(ctx, *Transaction) error
//       GetStreak(ctx, userID string) (*Streak, error)
//       SaveStreak(ctx, *Streak) error
//       Leaderboard(ctx, limit int) ([]LeaderboardEntry, error)
//   }
